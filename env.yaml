name: MyShittyNoteAnalyser
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.13
  - numpy
  - sounddevice
  - aubio                    # optional but recommended
  - portaudio                # needed by sounddevice
  - tk                       # Tcl/Tk for the GUI
  - pip
  - pip: